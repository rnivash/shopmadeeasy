@{
    ViewData["Title"] = "ShopMadeEasy";
}

@model ICollection<Product>

@foreach(var product in Model){
    <div>
        <img src="@product.ImageUrl" />
        <h2>@product.Name</h2>
        <span>@product.Price</span>
        <button>Add to cart</button>
    </div>
}
